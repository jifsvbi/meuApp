import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diario.reconstrucao',
  appName: 'Diário de Reconstrução',
  webDir: 'www',
};

export default config;
